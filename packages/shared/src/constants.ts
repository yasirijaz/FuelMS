export const APP_NAME = 'FuelMS' as const
export const APP_VERSION = '0.1.0' as const
export const APP_IDENTIFIER = 'com.wsh.fuelms' as const

/** Decimal precision for fuel quantities (litres). */
export const FUEL_QUANTITY_PRECISION = 3 as const

/** Decimal precision for monetary amounts (PKR). */
export const MONEY_PRECISION = 2 as const

/** Maximum characters for description fields. */
export const MAX_DESCRIPTION_LENGTH = 500 as const
