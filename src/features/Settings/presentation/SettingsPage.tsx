import { FeaturePlaceholder } from '@shared/components/FeaturePlaceholder'

export function SettingsPage() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Settings module boundary for future application preferences, permissions, and configuration."
    />
  )
}
